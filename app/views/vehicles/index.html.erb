<h1>List all vehicles</h1>
  <p>
    <button class="btn btn-primary"><%= link_to 'Create new vehicle', new_vehicle_path %></button>
  </p>
  <table class="table">
    <tr>
      <th>Make</th>
      <th>Model</th>
      <th>Year</th>
      <th>MPG</th>
      <th>Edit</th>
      <th>Show</th>
      <th>Delete</th>
    </tr>
    <% @vehicles.each do |vehicle|  %>
      <tr>
        <td><%= vehicle.make %></td>
        <td><%= vehicle.model %></td>
        <td><%= vehicle.yr %></td>
        <% if vehicle.mpg %>
          <td><%= vehicle.mpg %></td>
        <% else %>
          <td><button class="btn btn-dark"><%= link_to "Fetch MPG", fetch_mpg_vehicle_path(vehicle)%></button></td>
        <% end%>
        <td><button class="btn btn-dark"><%= link_to "Edit", edit_vehicle_path(vehicle) %></button></td>
        <td><button class="btn btn-dark"><%= link_to "Show", vehicle_path(vehicle) %></button></td>
        <td><button class="btn btn-warning"><%= link_to "Delete", vehicle_path(vehicle), method: :delete, data: {confirm: "Are you sure?"} %></button></td>
      </tr>
    <% end %>
  </table>
</div>
